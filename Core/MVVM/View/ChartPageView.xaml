<Page x:Class="NewCryptoApp.Core.MVVM.View.ChartPageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:NewCryptoApp.Core.MVVM.View"
      xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"

      xmlns:viewModel="clr-namespace:NewCryptoApp.Core.MVVM.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ChartPageView">
    <Page.DataContext>
        <viewModel:ChartPageViewModel/>
    </Page.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="173*"/>
            <ColumnDefinition Width="27*"/>
        </Grid.ColumnDefinitions>
        <ScottPlot:WpfPlot x:Name="WpfPlot1"/>
        <Border   BorderBrush="Bisque" Background="Beige" BorderThickness="2" CornerRadius="10"   Grid.Column="1">

            <StackPanel   HorizontalAlignment="Center">
                <Border  Margin=" 5 5 5 5" BorderBrush="Bisque" BorderThickness="2" CornerRadius="10">
                    <StackPanel>
                        <Label Content="From " HorizontalAlignment="Center"/>
                        <Label Content="{Binding From}"/>
                    </StackPanel>
                </Border>
                <Border  Margin=" 5 5 5 5" BorderBrush="Bisque" BorderThickness="2" CornerRadius="10">
                    <StackPanel>
                        <Label Content="To " HorizontalAlignment="Center"/>
                        <Label Content="{Binding To}"/>
                    </StackPanel>
                </Border>
                <Border  Margin=" 5 5 5 5" BorderBrush="Bisque" BorderThickness="2" CornerRadius="10">
                    <ComboBox ItemsSource="{Binding ChartType}" SelectedItem="{Binding SelectedChartType}" />
                </Border>


                <Button Background="Transparent" BorderBrush="Transparent" Command="{Binding Back}">
                    <StackPanel Orientation="Horizontal">
                        <Label  FontSize="15" Content="Back"/>
                        <Image Source="/Image/Icons/Back.png" />

                    </StackPanel>

                </Button>

            </StackPanel>
        </Border>
        
    </Grid>
</Page>
